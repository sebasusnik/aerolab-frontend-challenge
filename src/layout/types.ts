export enum ButtonVariants {
  Default = 'Default',
  Disable = 'Disable',
  Processing = 'Processing...',
  Skeleton = 'Skeleton',
}

export enum Amount {
  min = 1000,
  mid = 5000,
  max = 7500,
}
